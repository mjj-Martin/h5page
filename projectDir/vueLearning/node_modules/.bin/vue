#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../vue-cli/bin/vue" "$@"
  ret=$?
else 
  node  "$basedir/../vue-cli/bin/vue" "$@"
  ret=$?
fi
exit $ret
